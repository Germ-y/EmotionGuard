{
  "name": "emotionguard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:backend": "python -m uvicorn app.main:app --reload --app-dir backend --port 4000",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend"
  }
}
